# MVP 需求追踪矩阵

> 日期：2026-07-26
> 状态：实现已搭建，运行证据待用户执行

“已实现”只表示存在对应代码与测试，不表示已经运行通过。

| 需求 | 实现证据 | 测试证据 | 当前状态 |
| --- | --- | --- | --- |
| 潜在场景图确定性生成 | `src/tcmi/data/schema.py`、`generator.py` | `tests/test_generator.py` | 已实现，未运行 |
| 四类信息条件 | `text_token_ids`、`render_image`、`projection_contract` | generator/audit tests | 已实现，未运行 |
| entity/predicate/direction/joint 标签 | `SceneGraph.labels` | generator/audit tests | 已实现，未运行 |
| manifest、seed、hash、审计 | data generator、audit、oracle | `tests/test_audit.py` | 已实现，未运行 |
| 跨条件只改变投影 | latent digest 与 sample ID 跨条件检查 | `tests/test_audit.py` | 已实现，未运行 |
| image/text/multimodal/shuffled/conflict | `src/tcmi/training/trainer.py` | `tests/test_training_contract.py` | 已实现，未运行 |
| 参数、更新、样本、token、GPU 预算 | parameter audit、budget tracker、aggregate audit | training/aggregation tests | 已实现，未运行 |
| 两种轻量架构 | `tiny_vit`、`tiny_cnn` | `tests/test_models.py` | 已实现，未运行 |
| 三 seed 与可扩展五 seed | YAML matrix 与通用 matrix iterator | aggregation tests | 已实现，未运行 |
| checkpoint/日志配置哈希 | RunContext、checkpoint | 静态契约 | 已实现，未运行 |
| 冻结线性 probe | `src/tcmi/evaluation/probe.py` | `tests/test_probe.py` | 已实现，未运行 |
| unseen combination | heldout split 与 mask metric | generator/audit tests | 已实现，未运行 |
| missing/noisy/conflicting modality | feature corruption variants | probe contract | 已实现，未运行 |
| seed 统计、CI、交互和失败样例 | aggregate、probe failure writer | `tests/test_aggregation.py` | 已实现，未运行 |
| 单卡 4090 与 48 小时 | device auto、跨训练/probe 累计门禁 | budget contract | 代码已实现，硬件未验证 |
| CPU 生成与审计 | numpy generator/audit | tiny audit test | 已实现，未运行 |
| 无绝对路径 | 相对 config/output path | 静态扫描 | 已检查 |
| smoke 不进入正式证据 | config/evidence/decision gates | config/decision tests | 已实现，未运行 |
| 主张映射到文件与配置 | run/aggregate manifest 与 source path | aggregation contract | 已实现，未运行 |

## 仍不能宣告完成的阶段验收

- 冻结实验协议：未完成；
- 数据生成器审计：代码存在，尚未运行；
- 两架构三 seed 主矩阵：尚未运行；
- 公平预算核对：代码存在，尚无结果；
- 预注册阈值裁决：部分阈值尚未机器可读化；
- 缺陷与失败记录：已建立，运行失败待追加。

因此，仓库状态必须保持“实验进行中”。
