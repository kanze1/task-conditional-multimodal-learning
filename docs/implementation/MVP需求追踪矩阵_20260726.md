# MVP 需求追踪矩阵

> 日期：2026-07-26
> 最近更新：2026-08-02
> 状态：v1 smoke 已完成；v2 修复待运行验证

“v2 已实现”只表示修复存在对应代码与测试；新版 smoke 运行前不升级为运行证据。

| 需求 | 实现证据 | 测试证据 | 当前状态 |
| --- | --- | --- | --- |
| 潜在场景图确定性生成 | `src/tcmi/data/schema.py`、`generator.py` | `tests/test_generator.py` | v1 已运行；v2 已实现待运行 |
| 四类信息条件 | `text_token_ids`、`render_image`、`projection_contract` | generator/audit tests | v1 已运行；v2 entity 拆分待运行 |
| entity/predicate/direction/joint 标签 | `SceneGraph.labels` | generator/audit tests | v2 joint_graph 四分类已实现 |
| manifest、seed、hash、审计 | data generator、audit、oracle | `tests/test_audit.py` | v2 增加线性几何审计 |
| 跨条件只改变投影 | latent digest 与 sample ID 跨条件检查 | `tests/test_audit.py` | v1 已验证；v2 待运行 |
| image/text/multimodal/shuffled/conflict | `src/tcmi/training/trainer.py` | `tests/test_training_contract.py` | v1 smoke 已验证 |
| 参数、更新、样本、token、GPU 预算 | parameter audit、budget tracker、aggregate audit | training/aggregation tests | v1 smoke 已验证 |
| 两种轻量架构 | `tiny_vit`、`tiny_cnn` | `tests/test_models.py` | v1 smoke 已验证 |
| 三 seed 与可扩展五 seed | YAML matrix 与通用 matrix iterator | aggregation tests | 已实现，未运行 |
| checkpoint/日志配置哈希 | RunContext、checkpoint | training/checkpoint tests | v1 smoke 已验证 |
| 冻结线性 probe | `src/tcmi/evaluation/probe.py` | `tests/test_probe.py` | v1 已运行；v2 主任务几何已对齐 |
| unseen combination | heldout split 与 mask metric | generator/audit tests | v1 smoke 已验证 |
| missing/noisy/conflicting modality | feature corruption variants | probe contract | v1 smoke 已验证 |
| seed 统计、CI、交互和失败样例 | aggregate、probe failure writer | `tests/test_aggregation.py` | v1 smoke 已验证 |
| 单卡 4090 与 48 小时 | device auto、跨训练/probe 累计门禁 | budget contract | v1 smoke 已验证；pilot 仍需校准 |
| CPU 生成与审计 | numpy generator/audit | tiny audit test | v1 已验证；v2 临时审计通过 |
| 无绝对路径 | 相对 config/output path | 静态扫描 | 已检查 |
| smoke 不进入正式证据 | config/evidence/decision gates | config/decision tests | v1 裁决已验证 |
| 主张映射到文件与配置 | run/aggregate manifest 与 source path | aggregation contract | v1 已验证；v2 沿用 |

## 仍不能宣告完成的阶段验收

- 冻结实验协议：未完成；
- 数据生成器审计：v1 已完成，v2 尚未运行；
- 两架构三 seed 主矩阵：尚未运行；
- 公平预算核对：代码存在，尚无结果；
- 预注册阈值裁决：部分阈值尚未机器可读化；
- 缺陷与失败记录：已建立，运行失败待追加。

因此，仓库状态必须保持“实验进行中”。
