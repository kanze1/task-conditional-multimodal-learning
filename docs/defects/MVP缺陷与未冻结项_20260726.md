# MVP 缺陷与未冻结项

> 状态：进行中
> 日期：2026-07-26
> 最近更新：2026-08-02 数据生成与审计通过，GPU 训练尚未开始

## D-MVP-1：严格互补与 image-only 主指标存在逻辑边界

若 `joint_graph` 的一部分只存在于文本，冻结 image representation 不可能单独恢复该独立信息。当前实现将 joint_graph 的互补主检查放在 fused scope，并继续对 entity、predicate、direction 报告 image scope。

正式协议仍需确认这一主指标映射。不得把 fused 增益写成 image encoder 获得了不可见信息。

## D-MVP-2：架构与 seed 仅为 pilot 默认值

`tiny_vit`、`tiny_cnn` 和 `2601/2602/2603` 已进入配置，但尚未通过运行确认参数规模、稳定性与预算，也未冻结为 formal。

## D-MVP-3：首次运行验证进展

2026-07-31 至 2026-08-02 已确认：

- Python 3.12.8 项目环境可用，`pip check` 无损坏依赖；
- pytest 30 项全部通过，包含两个 encoder 的 CPU forward 和训练契约测试；
- smoke 数据生成完成，四个条件共 24 个 shard、按条件投影计 1,536 条样本；
- 数据审计 9 项不变量全部通过，oracle audit 通过，全部 manifest 与 shard 现时哈希一致；
- ruff 首次复核发现 `src/tcmi/__main__.py` 多余空行，已修复。

尚未执行 40 个训练 cell、120 个 probe job、聚合或裁决。因此当前状态是“数据审计完成，
实验进行中”。

## D-MVP-4：48 GPU 小时可行性尚无实测

代码记录并限制累计 GPU/wall-clock 时间，但完整矩阵的吞吐和显存尚未实测。必须先跑 smoke，再决定 pilot epoch 或矩阵是否需要在协议冻结前调整。

## D-MVP-5：正式环境锁尚未生成

`pyproject.toml` 已声明依赖范围，但由于没有安装依赖，本次不能诚实生成 `uv.lock`。进入 formal 前必须由实际解析环境生成、审查并提交锁文件。

## D-MVP-6：自动 Go 判定故意不完整

当前裁决器能检查证据等级、协议状态、矩阵完整性、预算和部分 complementary 阈值，但 irrelevant 显著缩小、shuffled 控制和 conflict-flip 的最终统计定义尚未机器可读化。因此即使未来误用 formal 标签，也不会自动宣告 Go。

## D-MVP-7：自然数据外部验证未实现

这是第一阶段明确非目标。synthetic MVP 通过也不能直接支持预训练 VLM 或自然数据主张。

## D-MVP-8：首次运行暴露 Windows 启动前检查问题

2026-07-31 的首次调用确认了三个启动层问题：

- 项目 `.venv` 尚未创建；当前 Conda base 的 Python 3.12.8 可作为创建它的解释器，
  但 `py` 启动器仅注册了不受项目支持的 Python 3.13；
- 旧脚本包含无 BOM 的 UTF-8 中文异常文本，在部分 Windows PowerShell 配置中显示为乱码；
- 旧脚本未显式检查原生 Python 进程的退出码，在 Windows PowerShell 5.1 中存在失败后继续
  执行同阶段后续命令的风险。

脚本已改为纯 ASCII 启动错误信息、明确的 3.11/3.12 环境创建指引、配置存在性检查和
逐命令退出码门禁。后续项目环境创建、测试、数据生成与审计已验证该启动路径可用。

## D-MVP-9：当前 PyTorch 环境不支持 CUDA

2026-08-02 复核环境为 `torch 2.13.0+cpu`，`torch.cuda.is_available()` 返回 `False`。
当前 `training.device: auto` 会选择 CPU；若直接启动训练，不能形成 RTX 4090 吞吐、显存或
GPU 预算证据。安装与本机 CUDA 兼容的官方 PyTorch wheel，并确认识别 RTX 4090 后，才能
开始 smoke 训练矩阵。

## D-MVP-10：editable install 元数据污染工作树状态

`pip install -e '.[dev]'` 在 `src/` 下生成
`task_conditional_multimodal_learning.egg-info/`，此前未被 `.gitignore` 覆盖，导致工作树
出现无关未跟踪文件。现已增加 `*.egg-info/` 规则；该目录保留为本地环境元数据，不进入 Git
或运行源码哈希范围。
