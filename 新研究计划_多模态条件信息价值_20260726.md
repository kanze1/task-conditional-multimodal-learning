# 新研究计划：多模态条件信息价值与结构表征

> 状态：新方向立项草案
> 日期：2026-07-26
> 最近更新：2026-08-03，纳入可测量信息价值叙事草案；不改变两周 Go/No-Go
> 目标投稿：ICLR 2027（以官方后续公布的时间与政策为准）
> 资源边界：优先按单张 RTX 4090、两周 Go/No-Go、可复现实验设计
> 与旧项目关系：复用图结构探针、数据处理经验和部分评估代码；不延续“GCL 是主要创新”的旧主线

## 1. 一句话研究问题

**多模态训练何时能学到比单模态训练更稳健、可迁移的结构表征，何时不会，甚至会因无关、冗余或冲突模态而产生负迁移？**

## 2. 原始直觉与必要修正

最初直觉是：

> 多种模态描述同一个世界。把它们叠加起来训练，模型应当更容易恢复稳定、底层的真实结构。

与产业研究者讨论后，这个命题必须改成条件命题：

> 多模态训练的收益不由模态数量决定，而取决于额外模态对当前任务目标是否提供可靠、互补且可利用的信息。

因此，本研究不再证明“多模态一定更好”，而是研究多模态收益的条件和失效边界。

## 3. 为什么用图结构验证

图结构提供一个可控的潜在世界：

- 节点表示实体；
- 边类型表示谓词或关系；
- 边方向表示主体—客体角色；
- 局部子图和路径表示更高阶结构。

图像与文本可以被视为同一潜在图的不同投影。我们可以精确控制每个模态暴露哪些结构变量，由此构造：

1. **冗余信息**：图像和文本都完整提供目标变量；
2. **图像独有信息**：目标只在图像中可辨；
3. **文本独有信息**：目标只在文本中可辨；
4. **互补/协同信息**：单个模态都不足，联合后才可辨；
5. **无关信息**：附加模态与任务目标无关；
6. **冲突信息**：附加模态对目标提供系统性错误线索。

这些条件能够直接检验“模态信息价值决定多模态收益”的根命题，而不是依赖事后解释真实数据中的相关性。

## 4. 概念定义

### 4.1 任务条件信息价值

对目标结构变量 \(Y_k\) 和两个模态 \(X_v, X_t\)，本研究关心的不是模态自身的信息量，而是：

- 它是否包含与 \(Y_k\) 相关的信息；
- 该信息是否超出另一模态已经提供的内容；
- 两个模态是否只有联合使用时才能恢复 \(Y_k\)；
- 该信息在训练分布变化后是否仍可靠。

受 Partial Information Decomposition 启发，可将信息关系区分为：

- unique：某一模态独有；
- redundant：两个模态重复；
- synergistic：只有联合后才出现；
- nuisance/conflict：与目标无关或误导。

第一阶段不在高维表征上直接估计 PID。我们通过可控数据生成器预先规定每种信息关系，并用单模态/联合模态的 Bayes 可辨识性和 oracle probe 验证生成器确实满足设计。

叙事升级草案进一步把“信息价值”操作化为相对于预注册 probe 族的 usable-information / `I_V`
剖面：它用于产生可证伪的训练前预测，不被当作高维表示上的 PID 真值。该扩展须在 pilot
与 formal 之间单独冻结，不改变当前四条件矩阵、matched-compute 或 Go/No-Go 阈值。

### 4.2 稳健结构表征

不再使用未定义的“更底层”。主要操作化为：

1. **结构可解码性**：冻结 encoder 后，线性 probe 能否恢复实体、谓词和方向；
2. **组合迁移**：未见实体—关系组合上的表现；
3. **扰动稳定性**：图像噪声、文本改写、模态缺失与模态冲突下的性能；
4. **任务迁移**：相同表示迁移到未直接训练的结构任务时的样本效率；
5. **捷径依赖**：移除高相关但非因果线索后性能下降多少。

## 5. 核心命题

### 命题 A：多模态收益具有任务条件性

当附加模态对目标结构变量提供互补或协同信息时，多模态训练得到的目标模态表示优于匹配的单模态表示；当附加模态只提供冗余或无关信息时，收益显著缩小。

- 若 A 不成立：原始研究 idea 基本应停止。
- 若只在一个数据集或一个任务成立：只能作为局部经验现象，不能投稿 ICLR 主会。

### 命题 B：冲突信息会产生可预测的负迁移

当附加模态在训练期稳定但错误地预测目标时，多模态训练会形成模态捷径，在冲突翻转、模态缺失或组合外推时低于单模态训练。

- 若 B 不成立但 A 成立：研究仍可继续，但主张缩小为“互补信息收益”，不能讲完整的条件价值规律。

### 命题 C：图结构成分具有不同的信息价值曲线

实体、谓词和方向对两种模态的依赖不同，因此多模态增益不应是统一常数，而应随结构目标和信息分配方式发生系统性交互。

- 若只观察到模型或数据规模效应，没有“任务 × 信息条件”的交互：图结构只是换皮 benchmark，研究需要停止或重构。

## 6. Strongest accept / reject case

### Strongest accept case

1. 提出并验证一个比“多模态通常更好”更精确、可证伪的条件规律。
2. 数据生成器具有已知信息结构，能够做因果操纵，而不是只对自然数据做相关性分析。
3. 规律跨结构变量、训练目标和模型规模成立，并能预测自然图文数据中的失败模式。
4. 结果解释 modality dominance、collapse、missing-modality fragility 和负迁移为何只在部分任务出现。

### Strongest reject case

1. “多模态是否有用取决于信息是否有用”是同义反复，没有新预测。
2. 合成图任务过于简单，不能说明真实 VLM。
3. 所谓信息价值只是由 caption 长度、标签泄漏或训练算力决定。
4. 单模态和多模态 baseline 的参数量、监督量或优化预算不公平。
5. 观察到的现象只是 fusion architecture 或某个 loss 的特例。
6. 自然数据验证仍然依赖同一个 Visual Genome 生成器。
7. PID 或互信息语言只用于包装，没有可靠测量或可证伪关系。

研究设计必须逐项防守以上拒稿理由。

## 7. 最小 Go/No-Go 实验

### 7.1 唯一生死判断目标

**判断“多模态相对单模态的结构表征增益”是否随任务条件信息结构发生稳定、方向正确且超出随机波动的变化。**

### 7.2 数据：可控双模态场景图

生成小型潜在场景图，并投影为：

- 视觉模态：程序化渲染的形状、颜色、位置和有向交互；
- 文本模态：受控但具有多模板变化的场景描述；
- 标签：节点身份、边类型、边方向和一个需要跨模态联合推断的图属性。

第一版只需要 50K 训练样本、5K 验证样本、5K 测试样本。所有信息条件共享相同潜在图分布，只改变模态投影，避免数据难度混淆。

### 7.3 信息条件

第一枪只保留四种：

1. redundant：两个模态都完整编码目标；
2. complementary：每个模态各自缺失一部分，联合后目标可辨；
3. irrelevant：文本保持长度和词频，但与目标独立；
4. conflict：文本与目标在训练期高相关，在测试期翻转。

### 7.4 训练条件

保持目标模态 encoder、参数量、更新次数和数据量一致：

1. image-only；
2. multimodal-aligned；
3. multimodal-shuffled；
4. multimodal-conflict。

主要评估冻结后的 image representation，避免把融合头的额外容量误当成表征收益。文本表示作为对称补充，不作为第一阶段主结果。

### 7.5 最小模型

- 一个小型 ViT/Transformer 双塔，便于多 seed；
- 一个不同归纳偏置的轻量 CNN/Text Transformer 组合；
- 三个 seed 完成 Go/No-Go；通过后再扩展为五个 seed。

旧仓库的 scratch CLIP-like 代码只能作为启动参考，必须重新整理配置、路径、数据契约和日志，不能直接复制旧实验口径。

### 7.6 主要指标

1. 冻结表示的线性 probe accuracy；
2. unseen-combination accuracy；
3. conflict-flip accuracy；
4. missing-modality / corruption robustness；
5. 多模态相对 image-only 的增益：
   \[
   \Delta_k(c)=P_k(\text{multimodal},c)-P_k(\text{image-only})
   \]
   其中 \(k\) 是结构任务，\(c\) 是信息条件。

主要统计对象是 `训练方式 × 信息条件 × 结构任务` 的交互，而不是单个最好分数。

### 7.7 Go / No-Go 阈值

#### Go

同时满足：

1. complementary 条件下，多模态在至少两个结构任务上稳定优于 image-only，均值增益不少于 5 个百分点；
2. 该增益超过三 seed 合并标准差的两倍；
3. irrelevant 条件下收益显著缩小，conflict-flip 下出现可重复的退化；
4. 结果在两种 encoder 归纳偏置下方向一致；
5. shuffled、参数量和更新预算控制不能解释结果。

#### 大改

- complementary 有增益，但只在一个任务或一个架构出现；
- 冲突效应明显，但互补收益不稳定；
- 线性 probe 有收益，但 OOD/组合迁移不提升。

此时只允许缩小命题，不进入自然数据大实验。

#### No-Go

任一情况成立即停止：

1. complementary 与 irrelevant 的增益无法区分；
2. 效应小于 2 个百分点且处于 seed 波动内；
3. shuffled caption 获得与有效 caption 相同的提升；
4. 结果主要由多模态模型参数量或训练 token 数解释；
5. 改变架构后效应方向翻转。

## 8. 第二阶段：机制验证

仅在 Go/No-Go 通过后启动。

### 8.1 可检验机制

候选机制不是“信息多所以更好”，而是：

> 训练目标优先编码跨模态稳定且对当前目标可辨的因素；当跨模态共同部分与任务目标错位时，模型会压制模态独有但有用的因素，或学习更便宜的模态捷径。

### 8.2 最小机制实验

1. **信息剂量曲线**：逐步改变文本中目标变量的可见概率；
2. **可靠性曲线**：逐步提高训练期文本噪声或冲突比例；
3. **梯度归因**：测量不同结构 probe 方向上的梯度投影；
4. **容量控制**：固定总参数量，改变 fusion/encoder 分配；
5. **优化控制**：固定样本数、更新数和总编码 token。

目标是得到可预测的单调或阈值关系，而不是再堆一个新 loss。

## 9. 第三阶段：自然数据外部验证

仅在受控规律成立后启动。

### 9.1 模型

- 至少一个 OpenAI CLIP；
- 至少一个 OpenCLIP；
- 优先在预训练 checkpoint 上做受控继续训练或轻量适配；
- 不再用 scratch 小模型承担真实 VLM 主张。

### 9.2 自然信息条件

从同一图像构造不同训练视图：

1. full relation caption；
2. entity-only caption；
3. predicate/role masked caption；
4. matched-length shuffled caption；
5. plausible conflicting caption；
6. paraphrased caption generator shift。

所有条件必须匹配图像集合、caption 数量、平均长度、更新数和负例数。

### 9.3 外部评估

P0：

- 一个官方关系/组合 benchmark；
- 一个非 Visual Genome、非同生成器 benchmark；
- 标准零样本分类或检索保持度。

候选包括 SVO-Probes、VALSE relation、SugarCrepe++ 和官方 ARO。Winoground保留为边界检查，不要求全面提升，但不能隐去退化。

### 9.4 必须加入的捷径控制

1. frozen-image；
2. frozen-text；
3. shuffled-image；
4. text-only；
5. image-only；
6. caption-length / lexical-overlap matched；
7. 至少 200 个样本的人审，报告视觉支持、反事实有效性、自然性和歧义。

## 10. Baseline 策略

### P0

1. image-only matched-compute；
2. text-only；
3. standard multimodal contrastive learning；
4. shuffled / irrelevant modality；
5. late fusion 或简单 concatenation；
6. oracle full-graph probe，用于确认数据可学。

### P1

1. modality dropout；
2. loss reweighting / gradient balancing；
3. 一个针对 modality collapse 或 modality robustness 的已发表方法；
4. 一个 hard-negative 方法，验证旧 GCL 现象是否只是训练信号质量的局部例子。

### 暂不做

- 大型生成式 MLLM；
- 大规模网页预训练；
- 新 fusion architecture 搜索；
- 多个复杂 PID estimator；
- 为了表格数量加入与核心命题无关的 benchmark。

## 11. 论文定位

优先定位为：

> **机制分析 + 可控诊断论文**

不是：

- 新的通用多模态架构；
- 新的 hard-negative 方法；
- 新 benchmark 堆砌；
- “图神经网络解决多模态”的方法论文。

候选标题：

1. **Usable Information, Not More Modalities: Predicting When Multimodal Training Helps**
2. **The Information-Value Profile: A Predictive, Controlled Account of Multimodal Representation Gains**
3. **From Redundancy to Conflict: A Controlled Study of Multimodal Information Value**

最稳主张：

> We test whether a pre-training measurable usable-information profile, resolved at the level of structural variables, predicts when multimodal training helps, saturates, or produces negative transfer.

不能提前声称：

- 得到了普适多模态信息定律；
- PID 在大模型内部被精确识别；
- 解决了 modality collapse；
- 对所有模态、架构和任务都成立。

## 12. 两周执行计划

### 第 1–2 天：协议冻结

- 明确四种信息条件的生成规则；
- 编写数据不变量检查；
- 冻结模型规模、训练预算、probe 和随机种子；
- 预注册 Go/No-Go 阈值。

### 第 3–5 天：最小实现

- 数据生成器；
- image-only 与标准 multimodal baseline；
- 冻结表示线性 probe；
- shuffled 与 conflict 控制。

### 第 6–8 天：第一架构三 seed

- 完成四信息条件主矩阵；
- 只看预注册指标；
- 排查标签泄漏、长度差异与预算不匹配。

### 第 9–10 天：第二架构复核

- 不调核心超参；
- 检查效应方向是否保持；
- 输出 interaction plot 和 seed 方差。

### 第 11–12 天：最小鲁棒性

- unseen combination；
- conflict flip；
- 一种图像扰动和一种文本改写。

### 第 13–14 天：裁决

- 对照 Go/No-Go 标准；
- 更新需求与缺陷文档；
- 只有通过才设计自然数据实验。

## 13. 预期产物

Go/No-Go 阶段只需要：

1. 一份冻结实验协议；
2. 一个确定性数据生成器和审计报告；
3. 两个架构、三 seed 的主矩阵；
4. 一张“多模态增益 vs 信息条件”的主图；
5. 一份失败案例与决策报告；
6. 可复现命令和环境锁定文件。

在阶段通过前，不写完整论文，不设计新方法，不做大规模 benchmark。

## 14. 与旧 GCL 项目的关系

### 可以复用

- entity / predicate / role 的任务拆解；
- same-entity 和 direction-sensitive probe 思路；
- 数据不变量审计经验；
- 多 seed、matched-compute、generator-shift 的实验纪律；
- Winoground/VALSE 等外部边界结果带来的 claim 约束。

### 不直接复用

- “结构敏感性层级是新发现”的贡献表述；
- “GCL 是主要技术创新”的定位；
- scratch CLIP-like 结果对预训练 VLM 的外推；
- Visual Genome 训练与同源 probe 评估形成的闭环；
- 单负例理论替代真实多负例优化机制的写法。

### GCL 的新位置

GCL 只作为一个可选案例：

> 当任务相关结构信息在标准训练目标中暴露不足时，targeted negatives 是否能改变条件信息利用？

只有它在新协议中提供了超出已发表 baseline 的机制证据，才进入正文；否则不再保留。

## 15. 参考方向

- NegCLIP / ARO：composition-aware negatives
  https://arxiv.org/abs/2210.01936
- SVLC：结构概念训练与预训练模型微调
  https://openaccess.thecvf.com/content/CVPR2023/html/Doveh_Teaching_Structured_Vision__Language_Concepts_to_Vision__Language_CVPR_2023_paper.html
- TripletCLIP：合成视觉—语言负例
  https://proceedings.neurips.cc/paper_files/paper/2024/hash/39781da4b5d05bc2908ce08e43bc6404-Abstract-Conference.html
- Partial Information Decomposition：unique / redundant / synergistic information
  https://www.mdpi.com/1099-4300/26/7/546
- Multimodal representation collapse
  https://arxiv.org/abs/2505.22483

这些工作意味着：新论文不能把“模态会互补或坍缩”当作发现本身，必须贡献可控、跨任务、能预测自然数据行为的条件规律。
