# 本地产物目录

该目录用于保存生成数据、训练运行、probe、聚合与裁决产物，默认不提交大型运行文件。

目录约定：

- `data/`：数据 shard、manifest、预览与审计报告；
- `runs/`：逐 cell 配置、日志、预算、checkpoint、probe 与失败样例；
- `aggregates/`：seed 汇总、置信区间、交互效应、主图与裁决。

论文主张所引用的正式产物需要额外建立不可变归档和索引，不能只依赖本地被忽略的文件。本项目协议尚未冻结，因此当前配置不会生成 `formal` 证据。
